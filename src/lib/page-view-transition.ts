export function writingTitleVtName(slug: string): string {
  return `writing-title-${slug}`;
}

export function supplyTitleVtName(productId: string): string {
  return `supply-title-${productId}`;
}

export function supplyImageVtName(productId: string): string {
  return `supply-image-${productId}`;
}
