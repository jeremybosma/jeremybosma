<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/images/favicon.png" />
    <meta property="og:image" content="/images/ogimage.png" />
    <meta name="theme-color" content="#fffff9" />
    <meta name="generator" content={Astro.generator} />
    <title>Jeremy Bosma</title>
    <meta
      name="description"
      content="Jeremy Bosma, a founding design engineer. Currently working on my vesselspro, a better maintenance solution for the maritime industry."
    />
    <meta
      name="keywords"
      content="Jeremy Bosma, design, developer, vesselspro, development"
    />
    <meta name="author" content="Jeremy Bosma" />
    <meta name="robots" content="index, follow" />
  </head>

  <body>
    <slot />
  </body>
</html>
<style is:global>
  @import url("https://fonts.cdnfonts.com/css/satoshi");

  :root {
    --satoshi: "Satoshi", sans-serif;
    /* Light mode */
    /* --primary-surface: #fff;
    --primary-text: #000000bf; */

    /* Dark mode */
    --primary-surface: #000;
    --primary-text: #e7e7e7;

    scroll-behavior: smooth;
    font-family: var(--satoshi);
  }

  body {
    display: flex;
    justify-content: center;
    align-items: center;
    background: var(--primary-surface);
    color: var(--primary-text);
  }

  div {
    animation: fade-in 0.5s ease-in-out;
  }

  @keyframes fade-in {
    0% {
      opacity: 0;
    }
    100% {
      opacity: 1;
    }
  }

  main {
    padding: 25px;
    max-width: 1100px;
  }

  a {
    text-decoration: none;
    color: var(--primary-text);
  }

  p,
  span {
    font-size: 20px;
  }

  .title {
    font-size: 24px;
    font-weight: 600;
  }

  @media (max-width: 767px) {
    main {
      padding: 25px;
    }

    p {
      font-size: 18px;
    }
  }

  @media (max-width: 480px) {
    main {
      padding: 25px;
    }

    p {
      font-size: 16px;
    }

    .title {
      font-size: 20px;
    }
  }
</style>
